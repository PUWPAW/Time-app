{
  "name": "time-app",
  "version": "1.0.0",
  "description": "Pure JS time app",
  "main": "index.js",
  "scripts": {
    "dev": "parcel ./src/index.html -p 3000 --open --no-source-maps --no-cache",
    "build": "parcel build ./src/index.html --no-source-maps --no-cache"
  },
  "author": "Ignatenko",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
